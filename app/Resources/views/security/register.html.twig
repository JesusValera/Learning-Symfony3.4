{% extends 'base.html.twig' %}

{% block body %}
    <br>

    {{ form_start(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.plainPassword.first) }}
    {{ form_row(form.plainPassword.second) }}

    {{ form_end(form) }}

    <br><a href={{ absolute_url("/users/login") }}>Login</a>
    <br>
    <br>
{% endblock %}